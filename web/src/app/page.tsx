import { Button } from "./components/button";
import {ArrowRight} from "lucide-react"

export default function Home() {
  return (
    <main>
      <Button>
        Enviar 
        <ArrowRight />
      </Button>
    </main>
  );
}
